# 《宮闈浮生》後端規格書

> 文件狀態：歷史概要版。可開工的資料庫 Schema 與完整 API Contract 已移至 [後端規格書_v0.8.md](./後端規格書_v0.8.md)、[schema_v0.8.sql](./backend-spec/schema_v0.8.sql) 與 [api_v1_v0.8.md](./backend-spec/api_v1_v0.8.md)。

> 版本：v0.7  
> 狀態：自架後端 MVP 開發基準  
> 技術方向：ASP.NET Core Web API + PostgreSQL  
> 前端：GitHub Pages 靜態 Web  
> 共用玩法依據：[遊戲企劃規格書_v0.6.md](./遊戲企劃規格書_v0.6.md)

---

## 1. 文件目的

本文件規範自架後端的服務邊界、資料模型、API、安全、排程、備份及營運需求。所有正式遊戲結果以後端資料庫與稽核紀錄為準。

後端必須支援約 200 名玩家、10 名管理員，且不把人數寫成遊戲規則上限。初期容量以 50 個同時活躍連線與短時間活動尖峰作設計基準。

---

## 2. 技術基線

### 2.1 建議版本與元件

- .NET 10 LTS、ASP.NET Core Web API。
- PostgreSQL 18；若主機環境暫不支援，可先用仍在官方支援期的 PostgreSQL 17。
- Entity Framework Core + Npgsql Provider。
- OpenAPI 文件作為前後端 Contract。
- 結構化日誌使用 `Microsoft.Extensions.Logging` 搭配 Serilog 或等效方案。
- 排程先以 ASP.NET Core `BackgroundService` 搭配 PostgreSQL 租約鎖實作；需求複雜後可評估 Quartz.NET 或 Hangfire。

### 2.2 部署拓撲

```text
玩家瀏覽器
  ├─ game.<網域>  → GitHub Pages
  └─ api.<網域>   → HTTPS 反向代理
                         └─ ASP.NET Core API
                                └─ PostgreSQL（不公開至 Internet）
```

建議以 Docker Compose 或等效服務管理方式部署：

- Reverse Proxy：Caddy、Nginx 或 IIS 擇一，負責 TLS、轉送及 Request Size 限制。
- API：非 Root 帳號執行，僅接受反向代理或內網流量。
- PostgreSQL：只綁定 localhost／私有網路，不開放公網 5432。
- 備份工作：獨立排程，輸出到主機外的加密儲存空間。

第一階段不需要 Redis、訊息佇列或 Kubernetes；只有實際量測顯示瓶頸時再增加。

---

## 3. 架構原則

### 3.1 服務型態

MVP 採模組化單體（Modular Monolith），避免在 200 人規模下過早拆微服務。專案可分為：

- `Api`：HTTP、驗證、OpenAPI、中介軟體。
- `Application`：Use Case、權限與交易邊界。
- `Domain`：角色狀態、遊戲規則、領域事件。
- `Infrastructure`：EF Core、PostgreSQL、LINE、通知、排程。
- `Worker`：若排程日後需獨立部署，可抽成同一 Solution 的 Worker Service。
- `Tests`：單元、整合、Contract 與負載測試。

### 3.2 模組

- Identity：LINE Login、帳號、Session。
- Characters：建角、立繪、能力、狀態、位階、死亡與重建。
- World：宮廷時間、地圖、居所、全域開關。
- Events：事件房、參與者、投稿、外部互動提交與結算。
- Economy：貨幣、俸祿、商店、道具、庫存與帳本。
- Relationships：NPC 與真人玩家關係。
- Reproduction：侍寢、懷孕、流產、待生池、出生。
- Intrigue：下毒、調查、狀態效果與死亡流程。
- Notifications：站內通知與公告。
- Administration：審核、補償、停權、雙人覆核、稽核。
- Scheduling：日結、章節、截止與失敗重試。

### 3.3 共通規則

- 伺服器時間與資料庫時間使用 UTC；顯示層再轉 Asia/Taipei。
- 所有隨機結果在伺服器產生並保存輸入、結果、演算法版本與操作者。
- 所有資源異動必須在同一資料庫交易內完成。
- 重要動作可重試但不可重複生效。
- API 不信任前端傳來的價格、角色權限、抽選結果、效果或目前數值。

---

## 4. 驗證、帳號與權限

### 4.1 LINE Login

1. `GET /api/v1/auth/line/start` 建立 `state`、PKCE／Nonce（依 LINE 支援流程）及短效登入狀態。
2. 瀏覽器導向 LINE。
3. `GET /api/v1/auth/line/callback` 驗證 `state`、交換 Token、驗證身分資料。
4. 以 LINE User ID 查找或建立帳號。
5. 建立伺服器 Session，使用 `Secure`、`HttpOnly`、適當 `SameSite` 的 Cookie。
6. 重新導向允許清單內的前端網址，不把長效 Token 放在 Query String。

LINE User ID 與 Token 屬敏感資料，不出現在一般日誌。若不需要長期呼叫 LINE API，不保存 Access Token；需要時以加密形式保存並記錄用途。

### 4.2 一帳號一角色

- `users.line_user_id` 設唯一索引。
- `characters` 對同一 `user_id` 只允許一筆非封存角色，使用 Partial Unique Index 保證。
- 死亡角色保持唯讀歷史；玩家重新填單時建立新角色，不覆寫舊角色。
- 新角色不繼承舊角色的貨幣、道具、關係、能力或非公開知識。
- 重建角色沒有保護期。

### 4.3 角色狀態

建議狀態：

`ApplicationPending → WaitingBirth | Active → Paused | Dead | Suspended → Archived`

- 皇子／公主核准後進入 `WaitingBirth`。
- 嬪妃等可直接開放身份核准後進入 `Active`。
- `Dead` 不可恢復為 Active；若屬管理誤操作，必須使用受稽核的更正流程而非直接改資料。
- `Paused` 用於正式請假；不等同死亡，也不是新角保護期。

### 4.4 管理員 RBAC

至少區分：

- `SuperAdmin`：帳號與系統設定；人數應極少。
- `CharacterReviewer`：建角、補件、待生審核。
- `GameMaster`：事件、判定、位階與關係。
- `EconomyManager`：商店、發放、補償。
- `Moderator`：公告、玩家處置、申訴。
- `Auditor`：唯讀查詢稽核與報表。

管理員仍須使用自己的 LINE 帳號登入，不共用帳密。大量補償、角色死亡、回溯交易、變更待生結果、授予最高權限等高風險動作應採雙人覆核。

---

## 5. API 規範

### 5.1 共通約定

- Prefix：`/api/v1`。
- HTTPS only，UTF-8 JSON。
- Request／Response 使用 Camel Case。
- 日期時間為 ISO 8601 UTC，例如 `2026-08-15T06:30:00Z`。
- 列表採 Cursor 或 Page 分頁；管理查詢必須限制最大筆數。
- 每個 Response 回傳或接受 `X-Request-Id`。
- 寫入操作接受 `Idempotency-Key`；至少在設定的有效期內重送同一 Key 時回傳相同結果。
- OpenAPI 需標示認證、狀態碼、範例、列舉與錯誤代碼。

### 5.2 成功與錯誤格式

成功可直接回傳 Resource；錯誤統一使用 Problem Details 擴充格式：

```json
{
  "type": "https://api.example.com/problems/heir-capacity-exhausted",
  "title": "目前沒有可用的待生皇嗣",
  "status": 409,
  "code": "HEIR_CAPACITY_EXHAUSTED",
  "requestId": "01J...",
  "errors": {}
}
```

`detail` 只能包含可對玩家公開的內容；內部例外放在受保護日誌。

### 5.3 端點群組

| Method／Path | 用途 |
|---|---|
| `GET /auth/line/start` | 開始 LINE Login |
| `GET /auth/line/callback` | LINE 回呼 |
| `POST /auth/logout` | 登出並撤銷 Session |
| `GET /me` | 帳號、角色、權限與待辦摘要 |
| `POST /character-applications` | 建立角色申請 |
| `GET /character-applications/current` | 目前申請狀態 |
| `GET /characters/me` | 自己的人物完整資料 |
| `GET /characters/{id}/public` | 可公開人物資料 |
| `GET /world/map` | 地圖、場景、全域狀態 |
| `GET /events` | 事件列表 |
| `GET /events/{id}` | 事件房資料 |
| `POST /events/{id}/posts` | 非同步事件投稿 |
| `POST /external-play-submissions` | 提交 LINE 群等外部互動紀錄 |
| `GET /market/items` | 商品列表 |
| `POST /market/purchases` | 購買 |
| `GET /inventory` | 庫存 |
| `POST /inventory/{entryId}/use` | 使用道具 |
| `GET /relationships` | 關係摘要 |
| `GET /reproduction/status` | 生育、名額與待生狀態 |
| `POST /reproduction/audience` | 申請符合規則的侍奉／侍寢 |
| `GET /notifications` | 通知列表 |
| `POST /notifications/read` | 批次標示已讀 |
| `/admin/*` | 審核、事件、經濟、處置、稽核 |

正式實作時需將 `/admin/*` 展開於 OpenAPI，不接受只有一個可執行任意命令的通用管理端點。

### 5.4 CORS、CSRF 與 Session

- CORS Allow Origin 僅列正式及明確的測試前端網域，不可使用 `*` 搭配 Credentials。
- Cookie 型 Session 的所有變更操作必須驗證 CSRF Token 或等效防護。
- Session 需有閒置與絕對到期時間，登出、停權及權限調整可立即撤銷。
- 多機部署前需持久化 ASP.NET Core Data Protection Keys；單機亦不得在每次重啟後無預期地使所有資料不可解密。
- 敏感管理操作可要求近期重新驗證。

---

## 6. 核心資料模型

所有資料表至少包含適用的 `id`、`created_at`、`updated_at`；具狀態轉換者另有 `version` 或等效並行控制欄位。主鍵建議使用 UUID v7、ULID 或資料庫可有效索引的同類型識別碼，全專案統一。

### 6.1 Identity 與角色

| 資料表 | 主要用途／欄位 |
|---|---|
| `users` | LINE 識別、顯示名、帳號狀態、最後登入 |
| `user_sessions` | Session Hash、到期、撤銷、裝置摘要 |
| `admin_role_assignments` | 使用者與管理角色 |
| `character_applications` | 申請內容、版本、狀態、補件、審核者 |
| `characters` | user、身份、性別、姓名、狀態、位階、portrait |
| `character_stats` | 能力、次級資源、版本 |
| `character_status_history` | 狀態轉換、原因、操作者 |
| `preset_portraits` | 官方資產 ID、適用身份、啟用狀態、版本 |
| `ranks`、`rank_history` | 位階定義與晉降紀錄 |
| `residences`、`character_residences` | 居所定義與入住歷史 |

### 6.2 事件與互動

| 資料表 | 主要用途／欄位 |
|---|---|
| `event_rooms` | 類型、狀態、規則版本、開始／截止／結算時間 |
| `event_participants` | 參與角色、角色別、加入狀態 |
| `event_posts` | 作者、內容、提交時間、編輯／撤回狀態 |
| `event_results` | 結算輸入、結果、GM、版本 |
| `external_play_submissions` | 外部互動摘要、證明、審核狀態 |
| `relationships` | 來源、目標、類型、數值、可見範圍 |
| `relationship_history` | 異動原因、事件與前後值 |

### 6.3 經濟與道具

| 資料表 | 主要用途／欄位 |
|---|---|
| `currencies` | 貨幣／點數定義 |
| `wallets` | 角色各貨幣目前餘額快照 |
| `ledger_entries` | 不可變帳本、正負金額、原因、關聯交易 |
| `item_definitions` | 道具、效果版本、可用條件 |
| `inventory_entries` | 角色、道具、數量、到期 |
| `inventory_transactions` | 取得、消耗、移轉、補償紀錄 |
| `market_offers` | 商品、售價、期間、庫存／限購 |
| `purchases` | 購買結果與 Idempotency Key |

帳本採 Append-only；不得直接改舊 Ledger Entry。更正以新的反向／補正紀錄完成，Wallet 快照必須可由帳本核對。

### 6.4 生育與待生池

| 資料表 | 主要用途／欄位 |
|---|---|
| `heir_wait_pool_entries` | 待生角色、進池、抽中／退出時間、狀態 |
| `audience_requests` | 侍膳／侍寢要求、資格快照、結果 |
| `pregnancies` | 母方、狀態、預定出生、保留名額、規則版本 |
| `births` | pregnancy、抽中待生角色、時間、隨機紀錄 |
| `offspring_links` | 出生角色與親子關係 |

不另存一個容易失真的「可用名額」數字；可用名額由有效待生角色數減去進行中且已保留的懷孕數計算，並在交易鎖內判定。

### 6.5 陰謀、狀態與通知

| 資料表 | 主要用途／欄位 |
|---|---|
| `intrigue_actions` | 下毒／調查等行動、目標、成本、秘密狀態 |
| `status_effects` | 中毒、受傷等效果與期限 |
| `deaths` | 死因、裁定、公開資訊、覆核 |
| `notifications` | 收件者、類型、Payload、已讀時間 |
| `announcements` | 全域公告、有效期間、發布者 |

### 6.6 營運與稽核

| 資料表 | 主要用途／欄位 |
|---|---|
| `audit_logs` | 操作者、動作、對象、前後摘要、IP／Request ID |
| `approval_requests` | 高風險操作與第二位覆核者 |
| `idempotency_records` | Key、使用者、端點、Request Hash、結果 |
| `outbox_messages` | 交易內產生、待發送的通知／領域事件 |
| `scheduled_jobs` | 排程定義、下一次執行、鎖定資訊 |
| `job_runs` | 每次執行、結果、錯誤、重試次數 |

稽核紀錄不得由一般管理介面修改或刪除。保存期限與個資政策需在上線前決定。

---

## 7. 關鍵遊戲流程

### 7.1 皇子／公主申請與待生

1. 玩家提交角色申請，身份為皇子或公主；申請階段選擇的男女皆只是申請角色本身的資料。
2. 管理員核准後，角色狀態改為 `WaitingBirth`，同一交易新增一筆有效待生池紀錄。
3. 玩家名稱對外顯示「姓名（待生）」。
4. 角色在實際出生前不得執行 Active 角色專屬行動。
5. 退出、停權或申請作廢時，依規則移出有效池並記錄原因。

### 7.2 侍寢與名額保留

規則：若無可用待生皇嗣，所有新侍寢暫停。

交易流程：

1. 鎖定全域生育控制列或使用 PostgreSQL Advisory Lock。
2. 查詢有效 `heir_wait_pool_entries` 數量。
3. 查詢進行中且已保留皇嗣名額的 `pregnancies` 數量。
4. `available = waiting - reserved`；若小於或等於 0，回傳 `409 HEIR_CAPACITY_EXHAUSTED`。
5. 驗證人物資格、冷卻、狀態與其他遊戲條件。
6. 若本次成功懷孕，在同一交易建立 Pregnancy 並保留一個名額。
7. Commit 後透過 Outbox 發通知。

前端顯示的名額只供參考，最後仍以交易內判定為準。

### 7.3 流產與名額釋放

- Pregnancy 轉為 `Miscarried` 時立即釋放其保留名額。
- 同一 Pregnancy 不得重複釋放；狀態轉換使用鎖或版本檢查。
- 名額恢復後，新侍寢自然重新可用，不需人工修改全域數字。

### 7.4 出生抽取

1. 鎖定 Pregnancy、生育控制與所有仍有效的候選池範圍。
2. 取出有效且未被抽中的待生角色；沒有候選者視為資料異常，停止結算並告警。
3. 使用密碼學安全亂數或已核准的公平抽選實作做等機率抽取。
4. 被抽中角色的身份決定出生性別；不再另外抽一次男女。
5. 同一交易建立 Birth、親子關係、把待生角色轉為 Active、池紀錄改為 Drawn、Pregnancy 改為 Completed。
6. 保存候選 ID 清單雜湊、抽選演算法版本及結果；只向玩家公開規則允許的資料。
7. Commit 後發送出生與全域通知。

### 7.5 死亡與重新建角

- 死亡裁定需原子化完成角色狀態、未完成行動、事件資格、道具效果及通知處理。
- 高風險管理裁定應進入雙人覆核；規則自動死亡則保存規則版本與輸入。
- 死亡角色資料不刪除，公開頁依隱私與劇情規則顯示。
- 同一 LINE 帳號可建立下一份角色申請，但資料完全分離且無新角色保護期。

### 7.6 購買與道具使用

- 伺服器查詢正式價格、限購、庫存與角色資格。
- 鎖定 Wallet／Inventory 相關列，在同一交易寫入 Ledger、Purchase 與 Inventory Transaction。
- 使用道具時，效果與道具定義版本一併保存，防止改版後無法追查。
- Idempotency Key 相同且 Request Hash 相同時回傳原結果；相同 Key 搭配不同內容回傳衝突。

---

## 8. 並行、一致性與隨機性

### 8.1 交易與鎖

- 待生名額、出生抽取、購買、道具消耗、死亡、管理補償皆使用資料庫交易。
- 競爭資源使用 `SELECT ... FOR UPDATE`、Advisory Lock 或等效策略，並固定鎖定順序以降低 Deadlock。
- 一般編輯使用 Optimistic Concurrency Token；衝突回傳 `409`，不可靜默覆蓋。
- Deadlock 或暫時性資料庫錯誤可有限次自動重試；重試仍受 Idempotency 保護。

### 8.2 Idempotency

- 購買、道具使用、事件結算、生育、補償等端點必須支援。
- Key 綁定使用者、端點與 Request Hash。
- 記錄處理中、成功與失敗狀態；同一交易保存正式結果。
- 保存期至少涵蓋使用者可能重送的期間，具體天數上線前決定。

### 8.3 Outbox

在遊戲資料 Commit 的同一交易寫入 Outbox，背景工作再發站內通知或其他整合，避免「資料成功但通知遺失」或反向情況。

### 8.4 隨機紀錄

- 不接受前端傳入的隨機結果。
- 每次結果保存用途、候選集合摘要、規則版本與結果。
- 除非產品明確要求可重播，不保存會使秘密事件可被玩家預測的公開 Seed。

---

## 9. 排程與背景工作

### 9.1 需要的工作

- 每日／每週資源與俸祿結算。
- 事件開啟、截止、逾期與結算提醒。
- 懷孕階段、出生時間與名額釋放。
- 中毒／狀態效果期限及死亡判定。
- 章節切換、全域狀態與公告。
- Outbox 通知派送。
- Session、Idempotency Record 與暫存資料清理。
- 稽核與備份結果檢查。

### 9.2 執行原則

- 排程定義及最後執行狀態保存在 PostgreSQL，不只存在記憶體。
- 每次工作有唯一執行 ID，使用租約或 Advisory Lock 防止多實例重複執行。
- 工作內容本身必須具 Idempotency；逾時或重啟後可安全重跑。
- 採指數退避與最大重試次數，超過後標記 Failed 並通知管理員。
- 顯示給玩家的截止時間以 Asia/Taipei 呈現，但比較與儲存使用 UTC。

---

## 10. 效能與容量

### 10.1 目標

- 規模：一般約 200 玩家、10 管理員，不設硬性玩家上限。
- 初期壓測：50 個同時活躍使用者，並模擬活動開始／截止的瞬間尖峰。
- 一般讀取 API P95 目標小於 500ms；一般寫入 P95 小於 1 秒，不含刻意的外部 LINE 回呼延遲。
- 事件列表、通知、帳本、稽核皆必須分頁。

### 10.2 資料庫

- 為 User、Character、狀態、事件、截止時間、通知未讀、Ledger Reference 建立實際查詢所需索引。
- 上線前以 `EXPLAIN ANALYZE` 檢查主要列表與管理查詢。
- 避免 N+1 Query；大 Payload 使用投影 DTO，不直接序列化 EF Entity。
- Connection Pool 設定需符合主機資源，API 不得無上限建立連線。

### 10.3 快取

- 初期只使用 HTTP／程序內短效快取保存公開且低風險資料，例如規則、地圖定義、商品圖片 Metadata。
- 餘額、庫存、名額、權限及角色狀態不得依賴可能過期的快取作正式判定。
- 多實例或量測顯示需求後再導入 Redis。

### 10.4 Rate Limiting

- 登入開始、登入回呼、投稿、購買、道具使用與管理端點分別設定限制。
- 依使用者 ID 為主、IP 為輔；反向代理需正確且安全地傳遞真實 IP。
- 超限回傳 `429` 與可用的重試資訊，不把一般輪詢誤判為攻擊。

---

## 11. 安全與隱私

- 密鑰以主機 Secret／環境注入，禁止提交 Git；Production 與測試使用不同憑證。
- 資料庫帳號採最小權限；Migration 帳號可與 Runtime 帳號分離。
- 所有輸入使用白名單驗證、長度限制與安全輸出編碼。
- 事件文字若支援 Markdown，使用安全解析器並過濾 HTML、Script 與危險連結。
- 附件不是 MVP；開放前另做類型、大小、惡意檔案與私密儲存設計。
- 密碼、Token、Cookie、LINE User ID、完整 Request Body 不寫入一般 Log。
- 錯誤回應不洩漏 Stack Trace、SQL、路徑、內部主機名。
- 管理 API 除 RBAC 外要記錄操作者、目標、原因、Request ID 與結果。
- 個資蒐集最小化；隱私政策需說明 LINE 識別、角色內容、日誌與保存／刪除方式。
- 定期更新 .NET、NuGet、PostgreSQL、OS 與反向代理安全修補。

---

## 12. 可觀測性與維運

### 12.1 日誌

- 使用 JSON 結構化日誌，至少包含 Timestamp、Level、Request ID、User／Character 的不可逆或內部 ID、Route、Duration、Result。
- 高風險 Domain Event 與 Audit Log 分開處理；應用日誌輪替不等於刪除 Audit。
- 設定保留與輪替，避免磁碟滿載。

### 12.2 健康檢查

- `/health/live`：程序是否存活，不依賴所有外部服務。
- `/health/ready`：資料庫、Migration 狀態及必要依賴是否可提供服務。
- 健康端點回應不得包含 Connection String 或例外細節。

### 12.3 監控與告警

至少監控：

- HTTP 5xx、429、P95 延遲與請求量。
- API 程序重啟、CPU、記憶體、磁碟空間。
- PostgreSQL 連線、鎖等待、慢查詢、磁碟與備份。
- 排程失敗、Outbox 堆積、出生／結算工作逾期。
- TLS 憑證到期。

告警先送管理團隊使用的營運管道；LINE 群遊戲聊天不宜作唯一告警來源。

---

## 13. 備份與災難復原

### 13.1 最低要求

- 每日自動 PostgreSQL 備份；備份完成不等於成功，需驗證可讀性與退出碼。
- 備份檔加密並複製到伺服器以外的位置，不能只留在同一顆硬碟。
- 建議保存：7 份每日、4 份每週、6 份每月；正式政策依成本與個資要求調整。
- 每季至少做一次隔離環境還原演練，記錄所需時間與問題。
- 美術資產、設定、Migration 與部署檔由 Git／資產儲存管理；密鑰另行安全備份。

### 13.2 進階選項

若營運後無法接受遺失一天內資料，啟用 PostgreSQL WAL Archive／Point-in-Time Recovery，並訂定：

- RPO（最多可遺失資料時間）目標。
- RTO（恢復服務所需時間）目標。
- 誰能執行還原及如何通知玩家。

建議 MVP 先以 RPO 24 小時、RTO 8 小時作最低討論起點，上線前由營運方確認是否足夠。

---

## 14. Migration 與發布

### 14.1 環境

- `Development`：本機與假 LINE 身分流程，可使用去識別測試資料。
- `Staging`：與正式相近、獨立資料庫及 LINE Channel。
- `Production`：正式資料，限制管理與資料庫存取。

不得把 Production 資料庫直接複製到開發機；需要除錯資料時先去識別化。

### 14.2 發布流程

1. Build、單元測試、整合測試及 Dependency Scan。
2. 建立不可變版本 Artifact／Container Image。
3. 備份並確認可回復點。
4. 執行向前相容 Migration。
5. 部署 API，檢查 Ready／Smoke Test。
6. 再發布依賴新 API 的前端。
7. 記錄版本、操作者、Migration 與回復方式。

破壞性 Schema 變更採 Expand／Migrate／Contract 分階段，不在同一次發布直接刪除舊欄位。

### 14.3 設定項目

正式鍵名可依專案慣例，但至少包含：

- PostgreSQL Connection String。
- LINE Channel ID／Secret 與 Callback URL。
- Frontend Allowed Origin／Redirect Allowlist。
- Session、Data Protection 與 CSRF 設定。
- 管理員 Bootstrap 流程。
- 日誌、Rate Limit、排程與備份設定。

敏感設定不得由可公開的前端 Repository 提供。

---

## 15. 測試與驗收

### 15.1 自動測試

- Domain Unit Tests：角色狀態、生育容量、出生抽取、死亡、貨幣與權限。
- Integration Tests：使用真正 PostgreSQL 測交易、索引、並行鎖與 Migration。
- API Tests：認證、CSRF、CORS、狀態碼、Problem Details、分頁。
- Concurrency Tests：多人同時搶最後名額、重複購買、同一 Pregnancy 重複結算。
- Security Tests：越權讀寫、管理角色、輸入內容、Session 撤銷、Rate Limit。
- Backup Restore Test：從備份建立新資料庫並跑完整性檢查。
- Load Test：50 活躍使用者及事件截止尖峰。

不要用 EF Core InMemory Provider 取代所有資料庫整合測試，因其無法反映 PostgreSQL 鎖、索引與交易行為。

### 15.2 關鍵驗收案例

- 同一 LINE User ID 無法同時建立兩個有效角色。
- 皇子／公主核准後進入同一待生池，男女不分池。
- 最後一個可用名額被並行請求時，只能有一個成功保留。
- 沒有可用待生皇嗣時，所有新侍寢均被拒絕。
- 流產只釋放一次名額；出生只抽中一名且等機率候選正確。
- 被抽中待生角色的既有性別即為出生性別。
- 玩家死亡後可重新申請，但舊資源完全不轉移，也不建立保護期。
- 相同 Idempotency Key 重送購買／道具／結算不會重複扣發。
- 管理員無法執行角色未授權的功能；高風險操作未覆核前不生效。
- API 重啟後排程可接續且不重複結算。
- 備份可以在隔離環境還原。

---

## 16. MVP 範圍

### 16.1 必做

- LINE Login、Session、唯一帳號與角色申請。
- 人物、位階、資源、角色狀態及官方立繪 Metadata。
- 事件房、非同步投稿、外部互動提交與管理結算。
- 宮市、庫存、Append-only 經濟帳本。
- 待生池、侍寢容量、懷孕、流產、出生抽取。
- 通知、公告、排程、Outbox。
- 管理 RBAC、雙人覆核基礎、Audit Log。
- PostgreSQL Migration、健康檢查、日誌、備份與還原文件。

### 16.2 暫不做

- 直接讀取或判分 LINE 群聊訊息。
- WebSocket 即時聊天室。
- 微服務、Kubernetes、Redis 與獨立訊息佇列。
- 玩家檔案／立繪上傳。
- 以 AI 自動裁定死亡、懲罰或高風險真人爭議。

---

## 17. 待確認事項

- 自架主機作業系統、CPU／RAM／磁碟、固定 IP 與上傳頻寬。
- 反向代理選 Caddy、Nginx 或 IIS，以及 TLS 自動更新方式。
- PostgreSQL 與 API 同機或分機；備份的異地位置與容量。
- 正式網域與 `game`／`api` 子網域。
- LINE Login Channel 是否已建立，以及隱私政策與 Callback URL。
- 管理員各角色名單及需要雙人覆核的完整操作表。
- RPO／RTO、備份保存期限、稽核與個資保存期限。
- 完整角色欄位、數值公式、位階、道具與事件資料內容。
