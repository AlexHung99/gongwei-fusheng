# 《宮闈浮生》前端規格書

> 版本：v1.0
> 修訂：2026-08-16，彙整目前 GitHub Pages 原型的新增、移除與待串接功能
> 修訂：2026-08-15，加入多場景視覺與玩家人物圖片上傳
> 修訂：2026-08-15，加入管理後台故事、人物稱號與遊戲設定中心
> 修訂：2026-08-16，將宮市庫存與道具使用整合至「我的人物」
> 修訂：2026-08-16，頂部改為 LINE 登入／註冊入口並精簡宮務與藏冊
> 修訂：2026-08-15，定稿建角、無限行動、現實日曆、投稿審核、稽核頁與 Buy Me a Coffee
> 狀態：v1.0 Web-first MVP 前端開發基準
> 玩家前台部署：GitHub Pages
> 管理後台部署：ASP.NET Core MVC／Razor Pages on IIS
> 後端介面：ASP.NET Core Web API on IIS
> 共用玩法依據：[遊戲企劃規格書_v1.0.md](./遊戲企劃規格書_v1.0.md)

---

## 0. v1.0 版本變更摘要

### 0.1 新增／保留功能

| 類別 | v1.0 決議與目前狀態 |
|---|---|
| 登入入口 | 頂部新增「LINE 登入」與「註冊」按鈕；LINE Login Channel「宮闈浮生」已建立，Channel ID 為 `2011123657`。目前只完成前端入口及 LINE Channel，後端 OAuth、Session、Callback 尚未實作 |
| 導覽 | 桌面左側固定導覽；左下只顯示自己的帳號、角色摘要與四能力標籤；手機使用底部導覽 |
| 人物 | 顯示本人立繪、身份、位階、狀態、資源、能力與標籤；體質 570 顯示「康健」；可看今日及歷史歷程 |
| 人物圖片 | 可選官方預設立繪或上傳自訂人物圖片；上傳圖具裁切預覽與 Pending／Approved／Rejected 審核狀態 |
| 玩家名冊 | 所有玩家依最近上線時間排序，支援搜尋、分頁、公開狀態、四能力標籤與今日／歷史歷程 |
| 事件 | 依宮城地點及玩家篩選；顯示行動者、時間、地點、消耗、結果與能力變化 |
| 宮城輿圖 | 依原始地圖文件呈現內務府、奉天樓、閱書院、太醫院、觀仙台、藏書閣；另有宮市與 NPC 名冊入口 |
| NPC | 宮城可查看 8 名 NPC 的設定、能力、故事與重新繪製立繪；嵐容華、黎良人及君疏鳶使用參考照片重新設計，君疏鳶不刻意高齡化 |
| 內務府 | 玩家可查看 10 名管理人員的姓名、職務、負責項目及最近上線時間，但不公開後台權限 |
| 宮市 | 收錄《遊戲規則／宮市》41 項商品；支援分類、搜尋、餘額判斷、一般購買及高風險申請提示 |
| 持有道具 | 整合於「我的人物」底下，顯示名稱、分類、效果、數量與最近購買時間；一般道具可確認使用，高風險道具送管理員裁決 |
| 管理後台 | React 畫面只作原型；正式後台由 ASP.NET Core MVC／Razor Pages on IIS 提供故事、稱號、設定、稽核與管理流程 |
| 贊助 | 右上保留 Buy Me a Coffee 醒目按鈕及站內說明彈窗；與遊戲能力、銀兩、道具、位階及審核完全無關 |

### 0.2 移除／不納入功能

| 功能 | v1.0 處理 |
|---|---|
| 全站搜尋 | 移除頂部搜尋欄，位置改為 LINE 登入與註冊入口 |
| 人物關係 | 不提供「人物關係」頁、關係圖或系統化關係數值 |
| 地圖主線劇情 | 從宮城輿圖移除；地圖改作行動地點、管理名單及 NPC 資訊入口 |
| 宮務與藏冊的宮市／庫存卡片 | 移除；宮市由地圖或人物頁進入，庫存直接整合於「我的人物」 |
| 獨立庫存主畫面 | 不提供 `/inventory` 玩家路由，不再維護第二套庫存介面 |
| 每日行動點 | 不設 Action Point 或每日補點；只保留個別玩法限制與截止時間 |
| LINE 群機器人核心流程 | LINE 群只作社群交流與人工宣傳，不作權威資料來源或必要遊戲介面 |
| GitHub Pages 正式管理後台 | 不提供；`/#/admin` 只保留 UI 原型，Production 管理操作必須進 IIS 後台 |

### 0.3 尚待後端完成

- ASP.NET Core API、PostgreSQL、IIS 部署目前尚未實作完成。
- LINE Login Callback 預定為 `https://api.<正式網域>/api/v1/auth/line/callback`；待正式後端網域與 HTTPS 完成後再填入 LINE Developers。Channel Secret 只存伺服器 Secret Store／環境變數，不寫入前端或本文件。
- 登入、註冊、建角、人物圖片審核、購買、庫存、使用、事件與歷程目前是前端互動原型；正式狀態、交易與 Audit 一律以後端為準。
- 正式 `game.<domain>`、`api.<domain>`、`admin.<domain>`、CORS、Cookie、CSRF、備份及監控仍待建置。

---

## 1. 文件目的

本文件規範玩家 Web 前台及管理後台的操作體驗。玩家前台為 React SPA 並部署於 GitHub Pages；正式管理後台由 `GongWei.Admin` 以 ASP.NET Core MVC／Razor Pages 實作並部署於 IIS。現有 React `/admin` 僅作介面原型，不是 Production 管理入口。

遊戲規則裁定、帳號驗證、抽選、交易、排程、資料保存與管理權限一律由後端負責；前端不得自行計算並宣告正式結果。

### 1.1 前後端責任邊界

| 項目 | 前端負責 | 後端負責 |
|---|---|---|
| 畫面與互動 | 顯示、輸入、導覽、動畫、錯誤提示 | 回傳可用狀態與正式結果 |
| LINE 登入 | 導向登入入口、呈現登入狀態 | OAuth/OIDC 流程、帳號綁定、Session |
| 人物數值 | 顯示 API 回傳值 | 計算、驗證、保存與稽核 |
| 抽選與隨機 | 播放結果動畫 | 產生、鎖定並記錄正式結果 |
| 道具與貨幣 | 顯示、送出使用或購買要求 | 庫存、扣款、效果與交易一致性 |
| 生育／待生池 | 顯示名額與角色狀態 | 名額鎖定、懷孕、出生抽取 |
| 管理操作 | 管理介面與確認視窗 | 權限驗證、雙人覆核、Audit Log |
| 贊助 | 顯示後端核准的外部連結 | 驗證 Buy Me a Coffee URL；不處理付款或遊戲回饋 |

---

## 2. 產品與技術定位

### 2.1 使用情境

- 一般玩家：約 200 人，主要以手機瀏覽器使用。
- 管理員：約 10 人，以手機與桌面瀏覽器操作。
- 一個 LINE 帳號同時只能有一個有效遊戲角色。
- LINE 群保留自由聊天與人工公告；正式遊戲操作、投稿、結果及角色資料均以 Web 為準。
- 玩家角色包含嬪妃、皇子與公主；可選官方預設立繪，亦可上傳自訂人物圖片送審。

### 2.2 建議前端技術

- React + TypeScript + Vite。
- 玩家端採靜態單頁應用程式（SPA），輸出至 GitHub Pages。
- 管理後台採 ASP.NET Core MVC／Razor Pages，共用 Application／Domain 層，不把管理功能編入玩家 SPA Bundle。
- CSS 可採 CSS Modules、Tailwind CSS 或團隊既有方案；第一版只選一套。
- API 狀態建議使用 TanStack Query；純介面狀態使用 React Context 或輕量 Store。
- 表單需具備 Schema 驗證，例如 Zod；後端仍須再次驗證。

上述套件屬實作建議，不構成玩法規則。若更換框架，仍須遵守本文件的路由、狀態與 API 邊界。

### 2.3 網域

建議使用已購買網域的子網域：

- `game.<網域>`：GitHub Pages 前端。
- `api.<網域>`：IIS 上的 ASP.NET Core API。
- `admin.<網域>`：IIS 上的 ASP.NET Core 管理後台。

前後端必須全程使用 HTTPS。正式環境不得直接顯示伺服器 IP 或未加密 HTTP 入口。

---

## 3. 使用者與角色狀態

前端啟動時先呼叫 `/api/v1/me`，再依後端回傳狀態決定首頁內容。

| 狀態 | 前端行為 |
|---|---|
| `anonymous` | 顯示 LINE 登入按鈕 |
| `account_only` | 引導建立／申請角色 |
| `application_pending` | 顯示審核中與補件通知 |
| `waiting_birth` | 顯示「待生皇嗣」、等待池資訊；不得進入一般角色行動 |
| `active` | 開放符合身份、位階與狀態的功能 |
| `paused` | 顯示請假／暫停原因，只開放允許的唯讀功能 |
| `dead` | 顯示角色生涯紀錄與重新填單入口 |
| `suspended` | 顯示停權訊息與申訴方式，不顯示敏感管理備註 |

畫面不得只靠隱藏按鈕作為權限控制；每次操作仍由後端驗證。

---

## 4. 資訊架構與路由

### 4.1 公開頁面

| 路由 | 畫面 | 說明 |
|---|---|---|
| `/` | 首頁 | 遊戲介紹、公告摘要、登入入口 |
| `/login` | 登入中繼頁 | LINE Login 狀態、錯誤與重試 |
| `/rules` | 規則 | 公開玩法、社群規範、隱私說明 |
| `/support` | 支援 | 常見問題、回報、申訴與可選的「請我們喝杯咖啡」外部按鈕 |

### 4.2 玩家頁面

| 路由 | 畫面 | MVP 功能 |
|---|---|---|
| `/onboarding` | 建角／申請 | 正式人設欄位、字數提示、草稿、官方立繪或自訂人物圖片、送審 |
| `/character` | 我的人物 | 僅顯示本人立繪、身份、位階、能力標籤、資源、狀態、今日／歷史歷程，以及宮市購入的持有道具與使用操作 |
| `/players` | 玩家名冊 | 依最近上線時間排序、搜尋、分頁；查看玩家公開狀態、能力標籤與今日／歷史歷程 |
| `/map` | 2D 宮廷地圖 | 六個規則地點、宮市、管理人員與 NPC 名冊入口、可執行行動與事件提示 |
| `/events` | 事件與公開歷程 | 依地圖地點及玩家篩選，顯示行動消耗、取得內容與數值結果 |
| `/events/:eventId` | 事件房 | 回合／串文、參與者、截止時間、投稿與結果 |
| `/market` | 宮市 | 商品、價格、購買、使用限制 |
| `/wardrobe` | 服儀 | 官方服裝／立繪差分選用與預覽 |
| `/offspring` | 生育／皇嗣 | 懷孕狀態、待生狀態、出生與子女紀錄 |
| `/chronicle` | 數值紀錄 | 本人晉升、事件、獎懲與其他異動時間線；逐項顯示原因、正負量及前後值 |
| `/notifications` | 通知中心 | 系統通知、待處理事項、已讀狀態 |
| `/settings` | 帳號設定 | 顯示名稱、通知偏好、登出；不可自行換綁 LINE |

### 4.3 管理員頁面

正式管理路由位於 `https://admin.<網域>/`，由 IIS 上的 ASP.NET Core Admin Web 提供；下列項目依後端 Policy 顯示：

- 儀表板：待審數、在線／近期活躍、系統警示、排程狀態。
- 角色審核：建角、補件、核准、拒絕、死亡後重建。
- 待生池與生育：池中人物、保留名額、懷孕與出生紀錄。
- 事件管理：建立事件房、參與者、截止、判定與結算。
- 故事編輯：故事線、章節、正文與分支節點；支援草稿、自動保存、玩家視角預覽、版本比較、發布與回復。
- 人物稱號：建立稱號、適用身份、可見性與樣式，並授予、設為主要稱號或撤回。
- 遊戲設定：依後端 Allowlist 顯示可調欄位、驗證規則、草稿與已發布值；高風險設定顯示雙人覆核狀態。
- 經濟管理：商品、發放、補償、異常交易查詢。
- 人物處置：請假、復歸、警告、停權、死亡裁定。
- 公告與通知：站內公告及需人工轉貼 LINE 的文字。
- 稽核查詢：獨立 `/Admin/AuditLogs` 頁，依操作者、動作、目標、日期及 Request ID 篩選；明細顯示理由與前後值，管理員不得修改或刪除。
- 系統狀態：版本、健康檢查摘要、排程失敗提示，不呈現密鑰。

GitHub Pages 玩家前台只顯示前往 `admin.<網域>` 的外部入口，且只作便利導覽；未顯示入口或知道網址均不影響後端授權結果。Production 不提供 GitHub Pages `/#/admin` 管理功能。

---

## 5. 導覽與版面

### 5.1 手機版

- 底部主導覽共 6 項：首頁、地圖、事件、玩家名冊、我的人物、更多。
- 主要行動按鈕需位於單手可操作區。
- 管理員入口放在「更多」內，連往 IIS `admin.<網域>`，並清楚區隔玩家與管理操作。
- 長篇事件文字採分段、角色頭像與時間標記，不模仿即時聊天室造成必須即時在線的壓力。

### 5.2 桌面版

- 左側固定導覽、中央主要內容、右側可放人物摘要與待辦。
- 頂部列不提供全站搜尋；右側固定呈現「LINE 登入」、「註冊」、Buy Me a Coffee 與通知按鈕。
- 管理表格支援搜尋、篩選、分頁及明確的確認流程。
- 不以滑鼠 Hover 作為唯一資訊來源。

### 5.3 全域元件

- 目前角色摘要。
- 通知未讀標記。
- 系統維護／侍寢暫停等全域公告。
- 載入、空資料、無權限、網路中斷、伺服器錯誤狀態。
- 具破壞性或不可逆操作的二次確認視窗。

---

## 6. 核心操作流程

### 6.1 LINE 登入

1. 玩家點擊頂部「LINE 登入」；「註冊」會先要求完成 LINE 登入，再導向建角填單。
2. 瀏覽器前往後端 `/api/v1/auth/line/start`。
3. 後端完成 LINE 驗證並建立安全 Session。
4. 後端重新導向前端指定頁面；網址不得夾帶長效 Access Token。
5. 前端呼叫 `/api/v1/me` 取得使用者與角色狀態。

前端不得儲存 LINE Client Secret、資料庫密碼或後端管理金鑰，也不得把長效登入憑證放入 `localStorage`。

### 6.2 建角與審核

1. 帳號無有效角色時顯示建角表單。
2. 玩家選擇宮妃、皇子或帝姬。宮妃欄位為姓、名、小字、生辰、年齡 15～18、容貌、性格、擅、不擅、喜、不喜、自介；皇嗣姓氏固定「蕭」、年齡固定 0。
3. 容貌顯示 60 字門檻；性格、擅、不擅、喜、不喜各顯示 50 字門檻；自介顯示 200 字門檻。未達門檻時可存 Draft，但不能 Submit。
4. 玩家選取官方預設立繪或上傳自訂人物圖片；兩者在 Submit 時必須二選一。
5. 自訂圖片須先完成前端格式、大小與裁切預覽，再由後端重新驗證、轉檔並進入圖片審核；審核中可保存建角草稿，但不得建立正式角色。
6. 皇子／帝姬申請經管理員核准後進入 `waiting_birth`，姓名後方顯示「待生」。宮妃核准後啟用。
7. 審核員填寫字數 35%、文筆 50%、邏輯 15% 評分並選起始位號；前端只顯示後端推導的體質、容貌、心計、福氣預覽。銀兩、威望、恩寵皆顯示 0，無行動點欄位。
8. 補件時只開放後端指定欄位，保留歷次申請紀錄。

### 6.3 事件房與真人互動

- MVP 採非同步事件串，不要求 WebSocket。
- 玩家可查看事件說明、參與者、回覆、截止時間與系統裁定。
- 編輯器提供「儲存草稿」與節流自動儲存，顯示最後儲存時間、字數及離線／失敗狀態；重新整理後從後端 Draft 還原。
- 「送出審核」前顯示角色身份及不可再編輯提示。送出後畫面改為「等待管理員審核」，只有退修後可繼續編輯。
- Approved 才出現在事件公開串；Rejected／Withdrawn 不公開。作者可查看自己的版本歷程與退修理由，其他玩家看不到編輯紀錄。
- LINE 群發生的角色扮演，不直接自動計分；玩家可透過 Web 表單提交摘要或證明，由管理員審核。
- 事件結算只顯示後端回傳結果，前端動畫不得提前暗示未確定結果。

### 6.4 高風險操作

下毒、道具使用、購買、侍寢、生育、死亡裁定、管理員補償等操作：

- 送出時顯示成本、目標與可能後果。
- 使用後端發出的 Idempotency Key 或 Client Request ID，避免連點重複執行。
- 送出後鎖定按鈕，等待正式回應；不得使用樂觀更新。
- 成功畫面顯示交易／操作編號，失敗則保留可理解的錯誤原因。

### 6.5 管理內容發布

1. ASP.NET Core 管理後台依 Admin Session 與 Policy 顯示故事編輯、稱號管理或遊戲設定分頁；View 隱藏不構成授權，Controller／Page Handler 必須再次驗證。
2. 故事編輯先保存 Draft，發布前顯示章節、節點、分支錯誤與玩家視角 Preview；發布成功才更新玩家內容。
3. 每次保存、發布、封存或回復均顯示 Version；遇到 `VERSION_CONFLICT` 不得覆蓋，應要求管理員重新載入並比較差異。
4. 稱號撤回與設定回復必須填原因；舊紀錄仍可在歷程中查看。
5. High Risk 設定送出後顯示「等待另一名管理員覆核」，不得先在前端呈現為已生效。

### 6.6 行動、日曆與月俸顯示

- 首頁、地圖及角色頁不得顯示「今日剩餘行動點」；需要顯示時使用「行動不限次數」。個別玩法的每日次數或冷卻獨立呈現。
- 宮廷日期由 `/world/state` 顯示，採 Asia/Taipei、現實 1 日等於宮廷 1 日；倒數計時仍以 API UTC Deadline 計算。
- 角色資源區顯示本月俸祿、最近發放 Ledger 及下次宮廷月首發放日；不在前端自行除以 12 或判斷年末補差額。

### 6.7 管理調整與稽核頁

- 銀兩調整、道具發放及補正 Modal 必填原因，送出前顯示角色、原值、變化量及預估結果；不顯示雙人覆核流程。
- 成功後顯示 Transaction ID 與 Audit ID，並提供「查看稽核紀錄」連結。
- `/Admin/AuditLogs` 預設依時間倒序，支援分頁與篩選；Before/After JSON 使用可折疊差異檢視，秘密欄位仍由後端遮罩。
- 歷代角色頁只存在管理後台；可從帳號明細看到所有在玩／死亡／封存角色。玩家公開人物頁不得顯示同一真人帳號的其他角色。

### 6.8 Buy Me a Coffee

- App 啟動時可與其他公開設定一起讀取 `/public-settings/support`。
- 桌面版在網站右上角、通知鈴鐺左側顯示醒目的黃色咖啡按鈕；手機版顯示咖啡圖示，需有 `aria-label`。此入口與遊戲導覽、任務及角色資源分離。
- 點擊後先開本網站 Modal，內容明確寫「自願支持營運，與遊戲角色、銀兩、能力、位階及審核無關」。Modal 內的主要 CTA 才使用 `<a target="_blank" rel="noopener noreferrer">` 開啟 Creator 頁。
- Creator URL 尚未設定時，右上說明按鈕仍可顯示，但 Modal CTA 顯示「贊助頁準備中」且不可點擊；不得導向 Buy Me a Coffee 平台首頁假裝是正式收款頁。
- 不載入第三方付款 Widget、不追蹤付款結果、不顯示贊助者遊戲身份，也不提供銀兩、道具、能力或審核優待。
- IIS 後台的遊戲設定頁提供啟用開關、Creator URL 與 1～30 字按鈕文字；URL 驗證失敗時不能發布。

### 6.9 生育設定畫面

- IIS 後台顯示「侍寢受孕率」0～100%，預設 100；「Pregnancy 天數」1～365，預設 10。
- 流產模式提供停用、事件觸發、數值門檻、每日機率四種；v1.0 預設選取「事件觸發」，並顯示「不進行每日隨機流產」。
- 這些規則屬 High Risk Setting：管理員先存 Draft，第二人核准發布後才影響新 Pregnancy。畫面須提示既有胎次不追溯。
- 玩家 Pregnancy 畫面顯示成立時間、預計生產時間、剩餘天數與目前可公開的風險狀態；不顯示秘密事件或後台私人理由。

---

## 7. API 串接規範

### 7.1 基本約定

- Base URL：`https://api.<網域>/api/v1`。
- 格式：UTF-8 JSON；圖片等靜態資產例外。
- 時間：API 一律使用 UTC ISO 8601，前端顯示為 Asia/Taipei。
- 金額、點數與數值不得以浮點誤差可能影響結果的方式自行計算。
- 跨網域要求使用 `credentials: include`；後端只允許正式前端 Origin。
- 寫入操作附帶 CSRF Token 與 `X-Request-Id`。

### 7.2 主要 API 群組

| 群組 | 用途 |
|---|---|
| `/auth/*`、`/me` | 登入、登出、目前使用者 |
| `/characters/*` | 建角、人物、能力、狀態、生涯 |
| `/portraits`、`/portrait-uploads/*` | 官方立繪、玩家圖片上傳、裁切與審核狀態 |
| `/world/*` | 地圖、居所、全域狀態 |
| `/events/*` | 事件列表、事件房、回覆、投稿 |
| `/market/*`、`/inventory/*` | 商品、購買、道具 |
| `/reproduction/*` | 侍寢、懷孕、待生池、出生資訊 |
| `/notifications/*` | 通知、已讀 |
| `/admin/*` | IIS 管理後台使用的具權限 API；GitHub Pages 玩家前台不可呼叫 |
| `/admin/story-*` | 故事線、章節、節點、預覽、版本與發布 |
| `/admin/title-*` | 稱號定義、授予、主要稱號與撤回 |
| `/admin/game-settings/*` | 遊戲設定草稿、發布、版本與回復 |

詳細端點、驗證與資料一致性以後端規格及 OpenAPI 文件為準。

### 7.3 錯誤處理

前端依 HTTP 狀態與錯誤代碼處理：

- `400`：欄位或規則不符，對應顯示欄位錯誤。
- `401`：Session 失效，引導重新登入。
- `403`：沒有權限或角色狀態不允許。
- `404`：資料不存在或無權查看。
- `409`：狀態衝突，例如名額已被占用、重複操作。
- `422`：資料格式正確但不符合遊戲規則。
- `429`：操作過於頻繁，顯示可重試時間。
- `500/503`：系統異常或維護，保留 Request ID 供回報。

錯誤訊息不得直接顯示 Stack Trace、SQL、伺服器路徑或密鑰內容。

### 7.4 資料更新

- 一般人物與列表：頁面進入時更新，可短暫快取。
- 通知與進行中事件：前景頁面每 30–60 秒輪詢；背景分頁降低頻率或暫停。
- 高風險操作完成後：立即重新抓取相關資料。
- 後端回傳版本或 ETag 時，前端應避免用舊畫面覆蓋新狀態。

---

## 8. 2D 視覺與資產規格

### 8.1 美術範圍

- 官方預設人物立繪與必要差分。
- 一張皇宮總覽地圖負責地點選擇；每個可進入地點另有場景背景，不得用同一張地圖圖代替所有場景。
- 正式規則地點以原始 `遊戲規則/地圖介紹.md` 為準：內務府、奉天樓、閱書院、太醫院、觀仙台、藏書閣。太液池、御花園、上林苑是觀仙台抽取子地點，不得取代核心節點。
- 宮城輿圖不提供「主線劇情」節點；資訊入口包含「內務府管理名單」與「宮中人物」。內務府名單對玩家公開管理員姓名、職務、負責範圍及最近上線時間，但不公開權限設定或後台功能。
- 地圖新增「宮市」，玩家可使用本人俸祿／銀兩購買《遊戲規則／宮市》六類商品；購買與使用分開留存歷程，高風險道具必須送管理員裁決。
- 宮市購買成功後立即更新人物頁持有道具與銀兩餘額；一般道具可確認後使用並扣除一件，需審核／高風險道具只送出使用申請，管理員裁決完成前不得扣除庫存。正式資料由後端交易與永久 Audit／人物歷程保存，前端不得自行判定效果。
- 地圖必須顯示地點用途、每日／全域次數及存取權限。正式地圖美術包含一張無文字的宮城鳥瞰總覽、六個規則地點與宮市專屬場景；鳥瞰圖的建築位置須與互動圖釘相符。原始輸出以橫式 3:2 構圖製作，前端採 WebP 最佳化版本。
- 原始 `NPC/附件` 內的人物照片僅作角色外觀、配色與氣質參考；正式網站使用經專案確認、重新繪製且可公開使用的原創 NPC 立繪。宮中人物名冊共 8 人，呈現姓名、封號、字、性格、擅長、喜惡、經歷、原始四項能力（若文件有提供）與人物故事。正式圖不得含原圖文字、標誌或浮水印，替換時須保留來源與核准紀錄。
- 嵐容華（陸馥錦）、黎良人（黎栖璇）、瑾惠太妃（君疏鳶）立繪以 `NPC/附件` 對應照片作外觀、配色與氣質參考後重新繪製；不得複製原圖文字、浮水印或精確構圖。嵐容華採黑金暖光，黎良人採青藍銀飾，君疏鳶採黑紅配色與自然成年女性形象，不刻意做高齡化處理。
- 四項能力依 `玩家數值` 文件即時計算文字標籤：體質（強韌／強健／康健／無恙／嬌弱／病態／逝世）、心計（莫測／高深／善謀／世故／直率／單純）、容貌（絕世／國色／花顏／端美／清秀／醜儀）、福氣（鴻運／祥瑞／福澤／如意／如願／霉運）。標籤由數值衍生，不另存可編輯文字。
- 玩家可選官方立繪，或上傳自訂人物圖片；自訂圖須通過內容與權利審核。

### 8.2 玩家人物圖片上傳

- 接受 JPEG、PNG、WebP；單檔最大 8 MB，原圖至少 600 × 800 px，建議直式半身或全身圖。
- 選檔後提供人物卡比例預覽、縮放與垂直位置調整；裁切值用 0–1 比例座標送往 API。
- 前端驗證只改善操作體驗，後端仍須檢查 Magic Bytes、尺寸、檔案大小，移除 EXIF 並重新編碼。
- 禁止真人照片、侵權素材、裸露、仇恨內容、聯絡資訊及冒充官方素材；送出後顯示 Pending，核准後才套用於正式人物。
- Rejected 顯示管理員原因並允許換圖；Withdrawn 不再顯示於公開人物卡。
- 上傳圖片由後端 Media Storage 提供，GitHub Pages 只保存官方資產，不保存玩家檔案。

### 8.3 檔案與載入

- 照片式背景優先 AVIF/WebP；需透明的立繪優先 WebP，必要時 PNG。
- 同一資產提供手機與桌面尺寸，使用 `srcset` 或等效方案。
- 首屏以外的地圖、背景與立繪使用 Lazy Load。
- 資產檔名使用版本化 Hash，確保更新後不讀到舊快取。
- `alt` 文字描述人物或功能；裝飾圖使用空 `alt`。
- 動畫尊重 `prefers-reduced-motion`。

### 8.4 初期效能預算

- 手機首頁首次下載目標小於 2 MB（不含使用者後續開啟的場景）。
- 單張一般背景建議小於 500 KB；立繪依實際尺寸壓縮。
- 玩家 JavaScript Bundle 不包含管理後台程式；管理 UI 由 ASP.NET Core Admin Web 獨立發布。
- 以中階手機與 4G 網路測試，主要內容應在 3 秒左右可操作；超出時需記錄原因。

---

## 9. 可及性與文字呈現

- 文字與背景對比至少符合 WCAG 2.1 AA 的一般閱讀需求。
- 所有表單有可見標籤、錯誤摘要與鍵盤操作能力。
- 不只用顏色區分成功、警告、死亡、停權等狀態。
- 正文預設字級不小於 16px，長文可切換較大字級。
- 古風字體只用於標題或裝飾，不影響正文辨識。
- 倒數與期限同時顯示絕對時間，例如「8 月 20 日 22:00」。

---

## 10. GitHub Pages 部署

### 10.1 建置與發布

- 原始碼放置於 GitHub Repository；Production Branch 受保護。
- GitHub Actions 執行安裝、Lint、Type Check、測試、Build 後發布 Pages。
- 正式 API URL 透過公開建置環境變數注入；任何前端變數都視為公開資訊。
- 自訂網域設定 CNAME，DNS 與 HTTPS 狀態列入上線檢查。

### 10.2 SPA 路由

GitHub Pages 不會原生將所有路徑交給 SPA，實作時二選一並寫入 README：

1. 使用 Hash Router，例如 `/#/events/123`，部署最單純；或
2. 使用 History Router 搭配 `404.html` 回退機制，並測試重新整理深層連結。

MVP 建議 Hash Router，以降低 GitHub Pages 路由問題；若品牌網址體驗優先，再改用經測試的 404 回退方案。

### 10.3 前端安全限制

- GitHub Pages 是公開靜態內容，Repository Secret 不得輸出到 Build Artifact。
- 不在原始碼或 Source Map 放入 LINE Secret、資料庫資訊、管理 Token。
- 正式 Source Map 是否公開需另行決定；至少不可包含密鑰。
- 設定基本 CSP、Referrer-Policy 與其他安全標頭時，需配合 GitHub Pages／反向代理可用能力；核心防護仍在 API。

### 10.4 IIS 管理後台

- `admin.<網域>` 使用 ASP.NET Core MVC／Razor Pages Layout、View Component、Tag Helper 與 AntiForgery，不由 GitHub Pages 發布。
- 登入 Cookie 與玩家 API Cookie 分離；管理 POST／PATCH／DELETE Handler 必須驗證 AntiForgery 與 Policy Authorization。
- 管理頁可直接共用 Application Use Case，不以 Loopback HTTP 呼叫同機 API，也不得由 Controller 直接操作 DbContext 跳過規則。
- Story Editor 的 Markdown Preview 仍須 Sanitization；Branch Rule 使用結構化表單，不提供任意程式碼編輯器。
- IIS Error Page 與 Production Logging 不得顯示 Stack Trace、Connection String、檔案實體路徑或未公開劇情資料。

---

## 11. 測試規格

### 11.1 自動測試

- 單元測試：格式化、權限顯示、狀態轉換、表單驗證。
- 元件測試：登入狀態、建角、事件投稿、購買與錯誤處理。
- E2E：玩家 GitHub Pages 登入回跳與角色／事件流程；另在 IIS Staging 測試 Admin Login、故事發布、稱號授予、設定覆核及 AntiForgery。
- API Contract：以後端 OpenAPI 驗證型別，避免欄位漂移。

### 11.2 瀏覽器與裝置

- 支援最近兩個主要版本的 Chrome、Edge、Safari、Firefox。
- 手機優先測試 iOS Safari 與 Android Chrome。
- 最小寬度以 360px 為基準；管理後台需支援 1366px 桌面。

### 11.3 MVP 驗收條件

- 玩家能以 LINE 登入並正確回到自己的唯一帳號。
- 無角色者能完成申請；皇子／公主核准後顯示待生狀態。
- 建角 Draft 可不完整保存；Submit 會正確驗證正式欄位與字數，核准後初始銀兩為 0、四能力符合起始位號。
- 玩家能選擇官方立繪或上傳自訂人物圖片，完成格式檢查、裁切預覽並看見審核狀態。
- 地圖可切換六個規則地點、宮市、管理人員名單與宮中人物入口；NPC 名冊可檢視 8 人設定、故事及核准立繪，手機版亦可操作。
- 玩家左側只呈現自己的帳號與角色摘要；「我的人物」可查看本人狀態、能力、資源，以及事件造成的正負異動與前後值。
- 人物能力 570 體質須顯示「康健」；所有玩家與 NPC 的四能力均依目前數值顯示正確級別。
- 玩家可在「我的人物」切換今日全部歷程與歷史紀錄，並保留日期範圍查詢欄位。
- 玩家可在「我的人物」查看宮市購入道具的名稱、類別、效果、數量及最近購買時間，選擇使用並完成確認；一般道具扣除數量，高風險道具送審但不先扣除。
- 事件頁可同時依地圖地點與玩家篩選，清楚顯示「誰、何時、在哪裡、消耗多少、得到什麼」。
- 玩家名冊依最近上線時間排序並分頁；公開詳情包含目前狀態、四能力標籤、今日歷程與歷史歷程。
- 宮市載入原始六類商品、價格與效果；餘額不足時不得送出購買，高風險品顯示管理員確認／裁決提示。
- 「宮務與藏冊」不提供人物關係、宮市或庫存卡片；宮市由地圖／人物頁進入，庫存整合於「我的人物」，NPC 資訊統一從宮城輿圖進入。
- 頂部不得顯示搜尋框；桌面與手機皆須提供清楚可辨識的 LINE 登入及註冊按鈕。
- 玩家可查看自己的角色、地圖、事件、庫存、通知與數值紀錄。
- 事件房可保存 Draft；Submit 後不可自行編輯，管理員 Approved 才公開，重新整理後內容不遺失、不重複。
- 高風險操作連點不會在畫面造成兩次成功結果。
- 管理員只能看到其權限允許的功能，重要操作具確認與結果編號。
- 內容管理員可保存與預覽故事草稿，發布後玩家才看得到；版本衝突不會覆蓋他人修改。
- 人物管理員可新增、授予與撤回稱號；每名人物同時只顯示一個主要稱號。
- 設定管理員只能修改後端 Allowlist 欄位，且無法從管理頁讀取 Secret；高風險變更必須等待覆核。
- 管理員調整銀兩或發放道具必須填理由，成功後能從 Audit 頁依 Actor、Target 與 Request ID 查到前後值。
- 玩家頁不顯示每日行動點；宮廷日期按台北時間 1:1 推進。
- 網站右上 Buy Me a Coffee 按鈕能開啟站內 Modal；未設定 Creator URL 時外部 CTA 停用，設定後才安全開啟外部頁，且不產生遊戲獎勵。
- 生育設定預設顯示受孕率 100%、Pregnancy 10 天與事件觸發流產；發布提示不追溯既有 Pregnancy。
- 手機、桌面及深層網址重新整理均可正常使用。
- API 無法連線時顯示可恢復的錯誤狀態，不破壞本地畫面。

---

## 12. MVP 與後續項目

### 12.1 MVP 必做

- LINE 登入與角色狀態導流。
- 建角／待生申請、官方立繪選擇與玩家人物圖片上傳送審。
- 本人人物總覽、數值異動歷史、2D 地圖、NPC 名冊、事件房與通知。
- 基本宮市與生育狀態畫面；庫存整合於「我的人物」，人物關係不列入 MVP。
- 管理員審核、事件、公告、待生池與稽核查詢介面。
- 故事／章節編輯、人物稱號管理與具版本的遊戲設定介面。
- 建角正式欄位與字數驗證、事件 Draft／審核佇列、歷代死亡角色管理頁、經濟／道具調整理由及 Audit 明細。
- 可由後台開關的 Buy Me a Coffee 外部支援按鈕。
- GitHub Pages 玩家前台自動部署；IIS 管理後台獨立 Release、健康檢查、回復流程、錯誤監測與基本可及性。

### 12.2 MVP 不做

- LINE 群訊息自動讀取、判分或同步成遊戲事件。
- 即時 WebSocket 聊天、語音、直播或大型同步戰鬥。
- 原生 iOS／Android App。
- 前端離線執行正式遊戲規則。

### 12.3 後續可評估

- PWA 安裝與 Web Push。
- Server-Sent Events 或 WebSocket 即時通知。
- 更完整的 2D 動畫、場景換裝與低階裝置模式。
- 事件編輯器、內容預覽環境與管理報表。

---

## 13. 待確認事項

- 正式網域名稱及 `game`、`api`、`admin` 三個子網域。
- 前端框架最終選擇與 Repository 是否公開。
- 官方立繪數量、授權來源、尺寸與差分規則；玩家上傳圖則依本文件規範審核。
- 是否採 Hash Router；若否，確認 GitHub Pages 的深層路由方案。
- 流產模式是否長期維持「事件觸發」，或改用數值門檻／每日機率。
- 正式 Buy Me a Coffee Creator URL；目前 URL 未設定時只顯示說明 Modal，外部 CTA 停用。
